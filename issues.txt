/flatten doesn't pass exit_order_time_ms (main.py:209) — the one path you missed, so it still uses the loose entry_bar_ts floor. It's a human-triggered close that can land long after the entry bar, which is exactly the case the tight floor was for.
ProcessLock.acquire() leaks its handle when refused — the except returns False without closing self._file. I hit this in testing: the lock file couldn't be deleted afterward. Harmless in production since sys.exit(1) follows immediately, but closing the handle in the except branch is one line.
get_funding_rate_history with startTime and no endTime — Bybit wants those as a pair and may ignore a lone startTime, returning the latest 50 instead. The f_ts > last_processed_funding_ts filter still makes it correct; it just means backfill silently caps at 50 settlements ≈ 16 days of downtime.
Funding is estimated as rate × notional, not read from the account's settlement records. That matches what backtest.py does — which is arguably the property you want — but §3.3's "realised funding" would come from the transaction log.